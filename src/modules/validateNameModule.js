/*
* not every operating system allows for every character to be in a file name
* filter to only allow letters and numbers (and spaces)
*/

// async function validateName(filename) {
//   return new Promise(resolve => {
//     resolve(filename.match(/[\w\s]/g).join(''));
//   })
// }

// const validateName = (filename) => new Promise((resolve, reject) => {
//   // return filename.match(/[\w\s]/g).join('');
//   resolve(filename.match(/[\w\s]/g).join(''))
// });

// exports.validateName = validateName;
